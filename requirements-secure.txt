# 🔒 SECURE Requirements for Trading Bot
# All packages pinned to secure, tested versions

# Core trading libraries
pandas==2.2.3
numpy==2.1.3
pyyaml==6.0.3

# Exchange APIs
pybit==5.12.0
ccxt==4.5.7

# Technical Analysis  
ta==0.11.0

# Data processing
python-dotenv==1.1.1
requests==2.32.5

# Security and crypto
cryptography==46.0.2
keyring==25.4.0

# Retry and error handling
tenacity==8.3.0

# Note: Decimal is built into Python standard library

# ML/AI (optional, secure versions)
scikit-learn==1.4.2
xgboost==3.0.5

# Database (if using)
psycopg2-binary==2.9.9
redis==5.2.0

# Monitoring and logging
prometheus-client==0.21.0
rich==13.9.4

# Telegram notifications
python-telegram-bot==21.7

# Configuration and CLI
click==8.1.7

# Testing and security scanning
pytest>=8.5.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
pytest-mock>=3.14.0,<4.0.0
bandit>=1.7.0,<2.0.0
pip-audit>=2.6.0,<3.0.0
safety>=3.2.0,<4.0.0

# Code quality
black>=24.0.0,<25.0.0
ruff>=0.8.0,<1.0.0
mypy>=1.13.0,<2.0.0

# Development tools
pre-commit>=3.10.0,<4.0.0

# Backtesting (if needed)
vectorbt>=0.25.0,<1.0.0

# Docker support (if using)
docker>=7.1.0,<8.0.0

# Performance and monitoring
aiohttp>=3.12.0,<4.0.0
aioredis>=2.0.0,<3.0.0

# Note: Always run `pip-audit` and `safety check` after installing
# Consider using `pip-audit --fix` for vulnerable dependencies
